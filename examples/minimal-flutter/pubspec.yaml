name: minimal_flutter_example
description: Fixture project that CI uses to verify flutter-golden-cycle templates render to valid Dart.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.2.0
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  path: ^1.9.0

flutter:
  uses-material-design: true
